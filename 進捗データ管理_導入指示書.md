# 進捗データ管理（エクスポート/インポート）導入指示書

このアプリに「進捗データ管理」機能を追加してください。
ブラウザのlocalStorageに保存している進捗データを、JSONファイルとしてバックアップ・復元できるようにします。

---

## 追加する機能

- **エクスポートボタン**：現在の進捗をJSONファイルとしてダウンロード
- **インポートボタン**：ダウンロードしたJSONファイルを読み込んで進捗を復元
- 復元前に確認ダイアログを表示する
- ファイルが無効な場合はエラーメッセージを表示する

---

## 実装手順

### ステップ1：既存コードの確認

以下の変数名・関数名を確認して、指示書末尾の「アプリごとの差し替え箇所」に記入してください。

| 確認項目 | 確認方法 |
|---|---|
| 初期状態の変数名 | `const INITIAL_STATE = ...` または `const initialState = ...` |
| localStorage のキー名 | `localStorage.setItem("???", ...)` の第1引数 |
| 状態を更新する関数名 | `setState(...)` または `dispatch(...)` など |
| ホーム画面のコンポーネント名 | アプリのメイン画面を表示する関数名 |

### ステップ2：ExportImportPanel コンポーネントの追加

以下のコンポーネントをコピーして、ホーム画面コンポーネントの直前（同じファイル内）に追加してください。
**`INITIAL_STATE`・`setState` は実際の変数名に置き換えてください。**

```jsx
function ExportImportPanel({ state, setState }) {
  const [showImport, setShowImport] = useState(false);
  const [importMsg, setImportMsg]   = useState("");

  const handleExport = () => {
    try {
      const blob = new Blob([JSON.stringify(state, null, 2)], { type: "application/json" });
      const url  = URL.createObjectURL(blob);
      const a    = document.createElement("a");
      a.href     = url;
      a.download = `backup-${new Date().toISOString().slice(0, 10)}.json`;
      a.click();
      URL.revokeObjectURL(url);
    } catch {
      alert("エクスポートに失敗しました");
    }
  };

  const handleImport = (e) => {
    const file = e.target.files?.[0];
    if (!file) return;
    const reader = new FileReader();
    reader.onload = (ev) => {
      try {
        const parsed = JSON.parse(ev.target.result);
        // 【差し替え】parsed の必須キーを検証する（例：progress, settings など）
        if (!parsed.progress) {
          setImportMsg("❌ 無効なバックアップファイルです");
          return;
        }
        if (window.confirm("バックアップから復元しますか？現在のデータは上書きされます。")) {
          setState({ ...INITIAL_STATE, ...parsed });
          setShowImport(false);
          setImportMsg("✅ 復元しました");
        }
      } catch {
        setImportMsg("❌ ファイルの読み込みに失敗しました");
      }
    };
    reader.readAsText(file);
    e.target.value = "";
  };

  return (
    <div style={{
      background: "#fff", borderRadius: 14, padding: "14px",
      border: "1px solid #e5e7eb", marginBottom: 12,
    }}>
      <div style={{ fontWeight: 800, fontSize: 13, marginBottom: 4 }}>
        💾 データ管理
      </div>
      <div style={{ fontSize: 11, color: "#888", marginBottom: 10, lineHeight: 1.6 }}>
        進捗はこのブラウザのみに保存されます。<br />
        定期的にバックアップして別デバイスでも使えます。
      </div>
      <div style={{ display: "flex", gap: 8 }}>
        <button
          onClick={handleExport}
          style={{
            flex: 1, padding: "9px 4px", fontSize: 12, fontWeight: 700,
            background: "#3B82F6", color: "#fff", border: "none",
            borderRadius: 10, cursor: "pointer",
            fontFamily: "inherit",
          }}
        >
          ⬇ エクスポート
        </button>
        <button
          onClick={() => { setShowImport(s => !s); setImportMsg(""); }}
          style={{
            flex: 1, padding: "9px 4px", fontSize: 12, fontWeight: 700,
            background: "#fff", color: "#3B82F6",
            border: "1.5px solid #3B82F6",
            borderRadius: 10, cursor: "pointer",
            fontFamily: "inherit",
          }}
        >
          ⬆ インポート
        </button>
      </div>
      {showImport && (
        <div style={{
          marginTop: 10, padding: "10px 12px",
          background: "#f9fafb", borderRadius: 8,
          border: "1px solid #e5e7eb",
        }}>
          <div style={{ fontSize: 12, color: "#555", marginBottom: 8 }}>
            バックアップ（.json）を選択してください
          </div>
          <input
            type="file"
            accept=".json"
            onChange={handleImport}
            style={{ fontSize: 12, width: "100%" }}
          />
        </div>
      )}
      {importMsg && (
        <div style={{
          marginTop: 8, fontSize: 12,
          color: importMsg.startsWith("✅") ? "#22c55e" : "#ef4444",
        }}>
          {importMsg}
        </div>
      )}
    </div>
  );
}
```

### ステップ3：ホーム画面に配置

ホーム画面コンポーネントの return 内、適切な位置（下部推奨）に以下を追加してください。

```jsx
<ExportImportPanel state={state} setState={setState} />
```

`state` と `setState` はアプリの実際の変数名に合わせてください。

---

## アプリごとの差し替え箇所

### 1. バリデーション（ステップ2内のコメント部分）

復元時、ファイルの正当性を確認する条件を変更してください。

```js
// 例1：progress キーがあるか確認
if (!parsed.progress) { ... }

// 例2：複数キーを確認
if (!parsed.progress || !parsed.settings) { ... }

// 例3：バージョン確認
if (!parsed.version || !parsed.data) { ... }
```

### 2. ファイル名

エクスポートファイル名の `backup` の部分をアプリ名に変更してください。

```js
// 変更前
a.download = `backup-${new Date().toISOString().slice(0, 10)}.json`;

// 変更後（例：家計簿アプリの場合）
a.download = `kakeibo-backup-${new Date().toISOString().slice(0, 10)}.json`;
```

### 3. INITIAL_STATE の変数名

アプリで使っている初期状態の変数名が異なる場合は置き換えてください。

```js
// 変更前
setState({ ...INITIAL_STATE, ...parsed });

// 変更後（例：initialState の場合）
setState({ ...initialState, ...parsed });
```

---

## デザインのカスタマイズ

ボタンの色はアプリのメインカラーに合わせてください。

```js
// エクスポートボタンの背景色
background: "#3B82F6",  // ← アプリのメインカラーに変更

// インポートボタンのボーダー・文字色
color: "#3B82F6",
border: "1.5px solid #3B82F6",  // ← 同じカラーに変更
```

---

## 注意点

- `useState` が使える環境（React 16.8以上）が必要です
- このコンポーネントは **Reactのクラスコンポーネントでは動きません**（関数コンポーネント専用）
- `window.URL.createObjectURL` はすべての主要ブラウザで動作します
- インポート後は `setState` によって localStorage への自動保存がトリガーされます（アプリがそのような設計の場合）。そうでない場合は `localStorage.setItem(KEY, JSON.stringify(parsed))` を別途呼ぶ行を追加してください

---

## 動作確認チェックリスト

- [ ] エクスポートボタンを押すと `.json` ファイルがダウンロードされる
- [ ] ダウンロードしたファイルをテキストエディタで開くと進捗データが読める
- [ ] インポートボタン → ファイル選択 → 確認ダイアログ → OKで進捗が復元される
- [ ] 無効なファイルを選択するとエラーメッセージが表示される
- [ ] インポート後にリロードしても進捗が保持されている
